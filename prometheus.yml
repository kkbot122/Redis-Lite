global:
  scrape_interval: 5s # Scrape the C++ nodes every 5 seconds

scrape_configs:
  - job_name: 'redis-lite-cluster'
    static_configs:
      # These use the Docker Compose service names and the metrics ports (port + 1000)
      - targets: ['node1:9001', 'node2:9002', 'node3:9003']