# Redis-lite Configuration

port=6379
leader_port=0
max_memory=1000
aof_file=database.aof
router_port=6379

# Leave empty to disable authentication
requirepass=

# Slowlog: log commands slower than this many ms (-1 to disable)
slowlog_threshold=10
slowlog_max_len=128